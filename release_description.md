# LGL Emoji Picker v1.0.4

Small usability release for the Qt 6 emoji picker.

Highlights:

- Hovering over an emoji now shows a tooltip with the emoji name.

Install on Fedora:

```sh
sudo dnf copr enable linuxgamerlife/lgl-emoji-picker
sudo dnf install lgl-emoji-picker
```
